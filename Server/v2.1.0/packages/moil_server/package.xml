<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>moil_server</name>
  <version>2.1.0</version>
  <description>
    The MoilCali rig server: motion stage, camera, calibration monitors, the whole
    calibration compute pipeline, the session store, 3D verification and the
    closed-loop calibration run. Everything that touches hardware or computes a
    number happens here; clients are remotes.
  </description>
  <maintainer email="oilmcut@moil.com.tw">MOIL</maintainer>
  <license>Proprietary</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>rclcpp_action</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>moil_interfaces</depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
